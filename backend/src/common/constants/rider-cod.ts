/** Max COD cash a rider may hold before new pickup orders are blocked (PKR). */
export const RIDER_COD_COLLECTION_LIMIT_PKR = 5000;

/** Riders only see / claim pickup pool orders within this radius of store/pickup (km). */
export const RIDER_NEARBY_ORDER_RADIUS_KM = 2;
